{
  "schema_v": 1,
  "_doc": "Rolling per-provider coverage ledger, appended by scripts/data_sentinel.py sweep (one entry per day, same-day sweeps overwrite). Used for day-over-day provider_drift detection (issuer coverage collapse, anti_patterns.md #7) and as the long-horizon record of which sources actually served data. Capped at provider_drift.ledger_max_entries in config/sentinel.json.",
  "entries": []
}
